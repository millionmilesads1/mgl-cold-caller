export const metadata = { title: "MGL Cold Caller", description: "Private cold call tracker" };
export default function RootLayout({ children }) {
  return <html lang="en"><body style={{ margin: 0, padding: 0, background: "#0a0a0a" }}>{children}</body></html>;
}
